[package]
name = "lelele-tests"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
lelele = { path = "../lelele" }

[dev-dependencies]
criterion = { workspace = true }

[[bench]]
name = "benches"
harness = false
